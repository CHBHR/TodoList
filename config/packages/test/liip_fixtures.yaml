liip_test_fixtures:
    keep_database_and_schema: false
    cache_metadata: false
    cache_db: ~

# create database with this command
#   bin/console --env=test doctrine:database:create
# create schema with bin/console 
#   --env=test doctrine:schema:update --force  
#   bin/console --env=test doctrine:migrations:migrate --no-interaction